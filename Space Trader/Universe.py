class Universe():
    isUniverse = None;

    def __new__(cls):
        
        if self.isUniverse == None:
        #THEN
            self.isUniverse = super(Universe, cls).__new__(cls)
        #END IF
        return isUniverse
    #END __new__

    def __init__(self):
        
